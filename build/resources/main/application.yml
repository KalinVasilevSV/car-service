spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/car-service-db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true"
    username: root
    password: sqlroot

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MySQL8Dialect